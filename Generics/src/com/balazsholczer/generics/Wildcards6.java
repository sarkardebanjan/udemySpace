package com.balazsholczer.generics;

public class Wildcards6 {

	/**
	 * 
	 * - the question mark ? -> represents an unknown type
	 * 		This ? is called wildcard !!!
	 * 
	 * - field variables + local variables   OK
	 *
	 * - The wildcards are never used as a type argument for a generic method invocation,
	 * 			 a generic class instance creation, or a supertype. 
	 * 					IMPORTANT !!!
	 * 
	 * 
	 */
}
